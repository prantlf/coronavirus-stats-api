#!/usr/bin/env node

const { ensureDatabase } = require('../lib/database')
const { updateAll } = require('../lib/update')
const { serveApi } = require('../lib/server')

ensureDatabase()
setInterval(updateAll, 60 * 60 * 1000)
serveApi()
